'use strict';

/**
 * dashboard-summary service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::dashboard-summary.dashboard-summary');
